# Base URLs of the two APIs. The browser calls them directly, so these are
# public (VITE_* is bundled into the client) — never put secrets here.
VITE_EXPRESS_API_URL=http://localhost:3001
VITE_NEST_API_URL=http://localhost:3002
